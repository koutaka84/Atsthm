<h2>ユーザー</h2>

 
<%= attachment_image_tag current_user, :profile_image, size: "50x50",format: 'jpg', fallback: "no_image.jpg" %>
<table class="table">

  <tbody>
		<tr></tr>
	<tr>
		<th>名前</th>
	  <th><%= current_user.name %></th>
	</tr>
	<tr><td><%#= link_to "ホーム", animal_path(current_user.id) %></td></tr>

	<tr>
		<td><%= link_to "編集", edit_user_registration_path, class: "glyphicon glyphicon-wrench edit_user_" %></td>
	</tr>

	<tr>
		<td>follows:<%= link_to user.followings.count,follow_path(user) %></td>
	</tr>
	<tr>
		<td>followers:<%= link_to user.followers.count,follower_path(user) %></td>
	</tr>
	<tr>
		<td><%= link_to '投稿', new_animal_path %></td>
	</tr>
	<tr>
		<td><%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: "glyphicon glyphicon-log-out" %></td>
	</tr>


  </tbody>
</table>