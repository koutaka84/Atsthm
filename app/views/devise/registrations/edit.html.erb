<h2>Edit <%= resource_name.to_s.humanize %></h2>

<%= form_for(@user) do |f| %>

        <h4>名前</h4>
        <%= f.text_field :name %>

        <h4>画像</h4>
        <%= f.attachment_field :profile_image %>
          <%= f.submit 'Update User' %>
<% end %>

<%= link_to "Back", :back %>
